CREATE TABLE users (
                       id INT PRIMARY KEY,
                       name VARCHAR(50),
                       email VARCHAR(100),
                       course VARCHAR(50),
                       age INT
);